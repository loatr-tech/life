{
  "name": "life",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "web": "yarn workspace web dev",
    "dev": "yarn web",
    "build:web": "yarn workspace web build",
    "build": "yarn build:web"
  }
}
